name: Integration Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 4 * * 1' # Weekly Monday 4am UTC
  workflow_dispatch:

jobs:
  integration:
    runs-on: ubuntu-latest
    services:
      streamline:
        image: ghcr.io/streamlinelabs/streamline:latest
        ports:
          - 9092:9092
          - 9094:9094
        options: >-
          --health-cmd "curl -f http://localhost:9094/health || exit 1"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'
      - name: Wait for Streamline
        run: |
          for i in $(seq 1 30); do
            curl -sf http://localhost:9094/health && break
            echo "Waiting for Streamline... ($i/30)"
            sleep 2
          done
      - name: Run integration tests
        run: go test -v -count=1 -tags=integration -timeout 120s ./...
        env:
          STREAMLINE_BOOTSTRAP: localhost:9092
          STREAMLINE_HTTP: http://localhost:9094
